# MultiInput
C# .NET8 application for sharing synced keyboard/controller inputs between a host and client(s). 

TODO:
- maybe use SDL for broader Controller support
- Tidy this damn spaghettio Code
- Make Handling Received Data use a Config that is initialized on Awake()
- Make Handling Sent Data use a Config that is initialized on Awake()
